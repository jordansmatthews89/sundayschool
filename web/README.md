The Next.js app now lives at the **repository root** (same level as `agent/`, `curriculum/`, etc.).

**Vercel:** Set **Root Directory** to **empty** (Settings → Build and Deployment → Root Directory → clear the override) so the build runs from the repo root.
